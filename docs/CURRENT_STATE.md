Current (2026-09-24): The accepted KCC movement, local PhysicsGrabber, HUD, crosshair, player prefabs, and both original test scenes remain unchanged. Friendslop.Network remains retired. `Assets/OnlyVolunteers/Network/` is the FishNet/Tugboat co-op prototype for up to four players. The owner runs KCC locally; FishNet now synchronizes the actual moving KCC child transform, and remote visuals follow it. Camera yaw and pitch drive only the remote visual. The server simulates eight shared Rigidbody boxes (5/20/50/100 kg) and validates exclusive LMB grabbing. NetworkTest now combines a larger arena, accepted movement labs, and co-op physics obstacles.

The two-PC Tailscale test of the previous build connected both users but found stationary remote bodies and unusable grabs. The stationary body was caused by NetworkTransform targeting the static player root, while KCC moved its child motor. The server grab range check also used that static root. Both paths were repaired without changing local movement or PhysicsGrabber. Unity 6000.5.11f1 compilation, Editor host spawn/movement/stop, and Windows development build succeeded. In separate localhost host + two-client processes, owner movement reached the server and observers, remote visuals followed position and camera yaw, and a shared box moved on the server and observer after the normal grab RPC path was invoked by a development probe. A competing grab was refused; holder disconnect released the box, which a second player could then grab. Real two-human movement and LMB interaction over Tailscale must be retested on this repaired build. Steam, lobby, and prediction are not implemented.
