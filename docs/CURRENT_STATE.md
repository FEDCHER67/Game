Current (2026-09-24): The accepted KCC movement, local physics interaction, HUD, crosshair, and both original test scenes remain unchanged. The retired Friendslop networking code remains removed. `Assets/OnlyVolunteers/Network/` now contains the first playable FishNet/Tugboat co-op slice in `NetworkTest`: host/client connection, up to four owned players, remote visuals, four server-simulated Rigidbody bodies, and server-validated exclusive LMB grabbing. This is a development prototype, without Steam, lobby, or prediction.

Verified: Unity 6000.5.11f1 compiled the new scripts; the Editor host spawned one owned player and four bodies, stopped cleanly, then restarted without duplicates. A Windows development build connected one host and two separate clients on localhost; all observed three players and four bodies. One client disconnected and reconnected into a free slot. A fourth participant joined, and a fifth was rejected. Server/client body authority and matching settled positions were recorded in separate logs. Automated gameplay input was unavailable for the separate builds, so two humans still need to check movement visibility and LMB grabbing, contention, release on disconnect, and reconnect interaction.
